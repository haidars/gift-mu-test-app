name: GitHub Actions deployment
on:
  push:
    tags:
      - "v*"

jobs:
  build:
    runs-on: gift-runner
    steps:
      - name: Checkout the code
        uses: actions/checkout@v3
      - name: Build the image
        run: |
          env